// src/app/dashboard/page.tsx

export default function DashboardPage() {
    return (
        <main className="mx-auto max-w-4xl px-4 py-10">
            <h1 className="text-2xl font-bold">Shortify Dashboard (coming soon)</h1>
            <p className="mt-2 text-sm text-slate-300">
                For now, use the main page to create jobs. This dashboard will later show
                clip results, stats, and account info.
            </p>
        </main>
    );
}
